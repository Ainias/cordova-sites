import { MenuAction } from "./MenuAction";
import { App } from "../../../App";
export declare class StartSiteMenuAction extends MenuAction {
    static _app: App;
    constructor(name: any, site: any, showFor: any, order: any, icon: any);
}
